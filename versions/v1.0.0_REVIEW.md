# E2E manual test review

Few feedback for you. 
1. Let's remove the display of today's spend and this monthly's spend and the API calls from /spend/logs entirely.
2. Turn to keep only using /v2/user/info to get this month's spend and use max_budget to calculate how many percentage has been used as we will set a budget_duration 30d for users.
3. Please change the current behaviour how the API call is triggered, please don't allow user to recall the API before the default refreshIntervalSeconds is hit.
4. When user clicks the status bar multiple time, don't trigger multiple call, just use the latest API call result to show the current spend of this monthly until the refreshIntervalSeconds is hit and automatically load the latest monthly spend.
5. Please review the necessary performance bottleneck, please be noted, there will be 150~200 users running their workspace simultaneously. If we didn't manage the underlying API calls properly, this will crash our LiteLLM Proxy easily.